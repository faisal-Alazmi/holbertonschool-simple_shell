# Simple Shell 0.3

This is a basic UNIX command interpreter written in C.

## Features
- Handles commands using the PATH variable.
- Does **not** call fork() if the command does not exist.
- Produces identical output to `/bin/sh` for errors.

## Compilation
