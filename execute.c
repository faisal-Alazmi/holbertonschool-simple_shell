#include "shell.h"

/**
 * execute_cmd - executes a command (placeholder)
 * @argv: NULL-terminated args
 *
 * Return: 0 (Task 1 skeleton)
 */
int execute_cmd(char **argv)
{
	(void)argv;
	return (0);
}
